[package]
name = "container-init"
version = "0.2.0"
edition = "2021"

[dependencies]
nix = { version = "0.31", features = ["process", "signal"] }
signal-hook = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
